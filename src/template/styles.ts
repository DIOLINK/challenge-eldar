import { Box, Button } from '@mui/material'
import { styled } from '@mui/material/styles'

export const RootContentForm = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
})
export const ButtonForm = styled(Button)({
  fontWeight: 'bold',
  letterSpacing: '0.03rem',
  minWidth: '100px',
  borderRadius: 22,
})
