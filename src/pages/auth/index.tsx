import { AuthComponent } from '@/components'
import { PageTemplate } from '@/template'

export default function Auth() {
  return (
    <PageTemplate>
      <AuthComponent />
    </PageTemplate>
  )
}
